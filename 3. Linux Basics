date
time
uptime
cd --> change directory
/ --> root
ls --> list all the files/directories
ls -lrt --> list all the files/directories by time
mkdir --> crete a directory
touch --> create a file
vi --> i --> edit the file --> esc (exit insert mode) --> :wq!
clear --> clear the screen
cat --> read a file
rmdir
rm
chmod --> permissions

Linux distributions:
CentOS - Amazon Linux, Redhat, Fedora - yum is the default package manager
Debian - Ubuntu - apt is the default package manager

//All Linux commands
pwd - Print Working Directory 
ls --> List the content of present working directory
//*******mkdir*******//
mkdir dir1 dir2 dir3 --> create multiple directories
mkdir dir1,dir2,dir3 --> no error but creates one directory
mkdir {dir1,dir2,dir3} --> create separate directories using ","
mkdir dir{1..9} --> to create multiple directories- creates 9 directories dir1, dir2..dir9
mkdir dir{a..d} --> creates multiple directories dira,dirb,dirc,dird
mkdir dir-{k..o} --> creates multiple directories dir-k,dir-l,dir-m,dir-n
mkdir rk-{1..5}{a..e} --> 5*5 directories would be created 
//*******subirectories*******//
2-ways to create subdirectories --> 
1: Go inside and create dircetory 
2.create cubdirectories by staying at home location
  mkdir abc/red abc/blue abc/yellow OR  mkdir abc/{blue,red,violet}--> create subdirectories in abc from home location 
  mkdir -p blue/green/red   -->create a subdirectory when parent directory is not present

cd ../../../ --> go back 3 directories back to previous dir -Specify ../ as many number of directories you need to go back
cd --> go back to home location
cd - --> got to previous working directory

